import {
  StyleSheet,
} from 'react-native'

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    justifyContent: 'flex-start',
    alignItems: 'center',
    marginTop: 50,
  },

  screenTitle: {
    fontSize: 30,
    textAlign: 'center',
    marginBottom: 30,
  },

  measurementFilter: {
    marginBottom: 30,
  },

  bigText: {
    fontSize: 70,
    fontWeight: 'bold',
    textAlign: 'center',
  },

  subtitleText: {
    textAlign: 'center',
  },
})
