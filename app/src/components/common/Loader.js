import React from 'react'
import { Bubbles } from 'react-native-loader'

import {
  View,
} from 'react-native'

const Loader = () => (
  <View>
    <Bubbles size={10} color="#d3d3d3" />
  </View>
)

export default Loader
